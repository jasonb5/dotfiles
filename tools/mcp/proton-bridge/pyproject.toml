[project]
name = "proton-bridge-mcp"
version = "0.1.0"
description = "MCP server for talking to Proton Mail Bridge over IMAP and SMTP"
requires-python = ">=3.12"
dependencies = [
  "mcp>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
