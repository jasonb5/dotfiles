[project]
name = "tasks-index-mcp"
version = "0.1.0"
description = "Read-only MCP server for local task index"
requires-python = ">=3.12"
dependencies = [
  "mcp>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
